import { Request, Response } from 'express';
import { createUser, getUser, updateUser, deleteUser } from '../services/UserService';

export const createUser = async (req: Request, res: Response) => {
  // Implementação do código para criar um usuário
};

export const getUser = async (req: Request, res: Response) => {
  // Implementação do código para obter um usuário
};

export const updateUser = async (req: Request, res: Response) => {
  // Implementação do código para atualizar um usuário
};

export const deleteUser = async (req: Request, res: Response) => {
  // Implementação do código para excluir um usuário
};
